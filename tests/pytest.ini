[pytest]
markers =
    offline: tests that run without cluster (pattern validation, etc.)
    integration: tests that require running services (scanner, target-agent)
    tekton: tests that require Tekton + running pipeline
testpaths = tests
asyncio_mode = auto
